# Solution walkthrough

Each group gets its own scale, so the second group can represent values around 2.2 without forcing the first group to use that larger range.

The mean absolute error is small here because the values are toy data. Real LLM quantization has harder outliers, matrix-multiplication kernels, packed storage, and calibration data.

