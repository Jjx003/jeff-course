# Solution walkthrough

The long-draft case accepts more tokens in expectation, but it also pays more draft cost. That is why production systems often tune draft length dynamically instead of using one fixed value.

